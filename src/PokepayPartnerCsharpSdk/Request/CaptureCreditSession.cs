// DO NOT EDIT: File is generated by code generator.
using System.Net.Http;
using System.Text.Json;
using System.Threading.Tasks;

using PokepayPartnerCsharpSdk;
using PokepayPartnerCsharpSdk.Response;

namespace PokepayPartnerCsharpSdk.Request
{
    public class CaptureCreditSession
    {
        private string SessionId { get; set; }
#if NETFRAMEWORK
        public string RequestId { get; set; }
#else
        #nullable enable
        public string? RequestId { get; set; }
#endif

        public CaptureCreditSession(string sessionId) =>
            (SessionId) = (sessionId);

        private string path { get { return "/credit-sessions" + "/" + SessionId + "/capture"; } }

        private static readonly HttpMethod method = new HttpMethod("POST");

#if NETFRAMEWORK
        public async Task<CapturedCreditSession> Send(Client client) {
                string res = await client.Send(path, CaptureCreditSession.method, this);
                return JsonSerializer.Deserialize<CapturedCreditSession>(res, client.JsonOptions);
        }
#else
#nullable enable
        public async Task<CapturedCreditSession?> Send(Client client) {
                string res = await client.Send(path, CaptureCreditSession.method, this);
                return JsonSerializer.Deserialize<CapturedCreditSession>(res, client.JsonOptions);
        }
#endif
    }
}
