// DO NOT EDIT: File is generated by code generator.
using System.Net.Http;
using System.Text.Json;
using System.Threading.Tasks;

using PokepayPartnerCsharpSdk;
using PokepayPartnerCsharpSdk.Response;

namespace PokepayPartnerCsharpSdk.Request
{
    public class GetUserDevice
    {
        private string UserDeviceId { get; set; }
#if NETFRAMEWORK
#else
#endif

        public GetUserDevice(string userDeviceId) =>
            (UserDeviceId) = (userDeviceId);

        private string path { get { return "/user-devices" + "/" + UserDeviceId; } }

        private static readonly HttpMethod method = new HttpMethod("GET");

#if NETFRAMEWORK
        public async Task<UserDevice> Send(Client client) {
                string res = await client.Send(path, GetUserDevice.method, this);
                return JsonSerializer.Deserialize<UserDevice>(res, client.JsonOptions);
        }
#else
#nullable enable
        public async Task<UserDevice?> Send(Client client) {
                string res = await client.Send(path, GetUserDevice.method, this);
                return JsonSerializer.Deserialize<UserDevice>(res, client.JsonOptions);
        }
#endif
    }
}
