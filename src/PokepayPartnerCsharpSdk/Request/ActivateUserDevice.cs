// DO NOT EDIT: File is generated by code generator.
using System.Net.Http;
using System.Text.Json;
using System.Threading.Tasks;

using PokepayPartnerCsharpSdk;
using PokepayPartnerCsharpSdk.Response;

namespace PokepayPartnerCsharpSdk.Request
{
    public class ActivateUserDevice
    {
        private string UserDeviceId { get; set; }
#if NETFRAMEWORK
#else
#endif

        public ActivateUserDevice(string userDeviceId) =>
            (UserDeviceId) = (userDeviceId);

        private string path { get { return "/user-devices" + "/" + UserDeviceId + "/activate"; } }

        private static readonly HttpMethod method = new HttpMethod("POST");

#if NETFRAMEWORK
        public async Task<UserDevice> Send(Client client) {
                string res = await client.Send(path, ActivateUserDevice.method, this);
                return JsonSerializer.Deserialize<UserDevice>(res, client.JsonOptions);
        }
#else
#nullable enable
        public async Task<UserDevice?> Send(Client client) {
                string res = await client.Send(path, ActivateUserDevice.method, this);
                return JsonSerializer.Deserialize<UserDevice>(res, client.JsonOptions);
        }
#endif
    }
}
