// DO NOT EDIT: File is generated by code generator.
using System.Net.Http;
using System.Text.Json;
using System.Threading.Tasks;

using PokepayPartnerCsharpSdk;
using PokepayPartnerCsharpSdk.Response;

namespace PokepayPartnerCsharpSdk.Request
{
    public class DeleteBank
    {
        private string UserDeviceId { get; set; }
#if NETFRAMEWORK
        public string BankId { get; set; }
#else
        public string BankId { get; set; }
#endif

        public DeleteBank(string userDeviceId, string bankId) =>
            (UserDeviceId, BankId) = (userDeviceId, bankId);

        private string path { get { return "/user-devices" + "/" + UserDeviceId + "/banks"; } }

        private static readonly HttpMethod method = new HttpMethod("DELETE");

#if NETFRAMEWORK
        public async Task<BankDeleted> Send(Client client) {
                string res = await client.Send(path, DeleteBank.method, this);
                return JsonSerializer.Deserialize<BankDeleted>(res, client.JsonOptions);
        }
#else
#nullable enable
        public async Task<BankDeleted?> Send(Client client) {
                string res = await client.Send(path, DeleteBank.method, this);
                return JsonSerializer.Deserialize<BankDeleted>(res, client.JsonOptions);
        }
#endif
    }
}
