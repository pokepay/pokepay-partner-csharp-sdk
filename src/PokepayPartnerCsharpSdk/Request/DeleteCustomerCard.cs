// DO NOT EDIT: File is generated by code generator.
using System.Net.Http;
using System.Text.Json;
using System.Threading.Tasks;

using PokepayPartnerCsharpSdk;
using PokepayPartnerCsharpSdk.Response;

namespace PokepayPartnerCsharpSdk.Request
{
    public class DeleteCustomerCard
    {
        private string CustomerId { get; set; }
#if NETFRAMEWORK
        public string CardUuid { get; set; }
#else
        public string CardUuid { get; set; }
#endif

        public DeleteCustomerCard(string customerId, string cardUuid) =>
            (CustomerId, CardUuid) = (customerId, cardUuid);

        private string path { get { return "/customers" + "/" + CustomerId + "/cards" + "/delete"; } }

        private static readonly HttpMethod method = new HttpMethod("POST");

#if NETFRAMEWORK
        public async Task<CustomerCardDeleted> Send(Client client) {
                string res = await client.Send(path, DeleteCustomerCard.method, this);
                return JsonSerializer.Deserialize<CustomerCardDeleted>(res, client.JsonOptions);
        }
#else
#nullable enable
        public async Task<CustomerCardDeleted?> Send(Client client) {
                string res = await client.Send(path, DeleteCustomerCard.method, this);
                return JsonSerializer.Deserialize<CustomerCardDeleted>(res, client.JsonOptions);
        }
#endif
    }
}
