// DO NOT EDIT: File is generated by code generator.
using System.Net.Http;
using System.Text.Json;
using System.Threading.Tasks;

using PokepayPartnerCsharpSdk;
using PokepayPartnerCsharpSdk.Response;

namespace PokepayPartnerCsharpSdk.Request
{
    public class ShowTransactionGroup
    {
        private string Uuid { get; set; }
#if NETFRAMEWORK
#else
#endif

        public ShowTransactionGroup(string uuid) =>
            (Uuid) = (uuid);

        private string path { get { return "/transaction-groups" + "/" + Uuid; } }

        private static readonly HttpMethod method = new HttpMethod("GET");

#if NETFRAMEWORK
        public async Task<TransactionGroup> Send(Client client) {
                string res = await client.Send(path, ShowTransactionGroup.method, this);
                return JsonSerializer.Deserialize<TransactionGroup>(res, client.JsonOptions);
        }
#else
#nullable enable
        public async Task<TransactionGroup?> Send(Client client) {
                string res = await client.Send(path, ShowTransactionGroup.method, this);
                return JsonSerializer.Deserialize<TransactionGroup>(res, client.JsonOptions);
        }
#endif
    }
}
