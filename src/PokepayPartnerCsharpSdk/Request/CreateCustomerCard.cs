// DO NOT EDIT: File is generated by code generator.
using System.Net.Http;
using System.Text.Json;
using System.Threading.Tasks;

using PokepayPartnerCsharpSdk;
using PokepayPartnerCsharpSdk.Response;

namespace PokepayPartnerCsharpSdk.Request
{
    public class CreateCustomerCard
    {
        private string CustomerId { get; set; }
#if NETFRAMEWORK
        public string Token { get; set; }
        public System.Nullable<bool> IsCardholderNameSpecified { get; set; }
#else
        public string Token { get; set; }
        #nullable enable
        public bool? IsCardholderNameSpecified { get; set; }
#endif

        public CreateCustomerCard(string customerId, string token) =>
            (CustomerId, Token) = (customerId, token);

        private string path { get { return "/customers" + "/" + CustomerId + "/cards"; } }

        private static readonly HttpMethod method = new HttpMethod("POST");

#if NETFRAMEWORK
        public async Task<UserCard> Send(Client client) {
                string res = await client.Send(path, CreateCustomerCard.method, this);
                return JsonSerializer.Deserialize<UserCard>(res, client.JsonOptions);
        }
#else
#nullable enable
        public async Task<UserCard?> Send(Client client) {
                string res = await client.Send(path, CreateCustomerCard.method, this);
                return JsonSerializer.Deserialize<UserCard>(res, client.JsonOptions);
        }
#endif
    }
}
