// DO NOT EDIT: File is generated by code generator.
using System;
using System.Net;
using System.Net.Http;
using NUnit.Framework;
using System.Threading.Tasks;
using System.Collections.Generic;

using PokepayPartnerCsharpSdk;

namespace PokepayPartnerCsharpSdk.Test
{
    public class TestGetCvsAuthorizations
    {
        private Client client;

        [OneTimeSetUp]
        public void OneTimeSetup()
        {
            client = new Client("~/.pokepay/test-config.ini");
        }

        [Test]
        public async Task GetCvsAuthorizations0()
        {
            try {
                Request.GetCvsAuthorizations request = new Request.GetCvsAuthorizations(
                    "c97b3de0-d02b-4a56-a4b6-d3539153e140"
                );
                Response.PaginatedCvsAuthorizations response = await request.Send(client);
                Assert.NotNull(response, "Shouldn't be null at least");
            } catch (HttpRequestException e) {
                Assert.AreNotEqual((int) e.Data["StatusCode"], (int) HttpStatusCode.BadRequest, "Shouldn't be BadRequest");
                Assert.True((int) e.Data["StatusCode"] >= 300, "Should be larger than 300");
            }
        }

        [Test]
        public async Task GetCvsAuthorizations1()
        {
            try {
                Request.GetCvsAuthorizations request = new Request.GetCvsAuthorizations(
                    "c97b3de0-d02b-4a56-a4b6-d3539153e140"
                ) {
                    PerPage = 14,
                };
                Response.PaginatedCvsAuthorizations response = await request.Send(client);
                Assert.NotNull(response, "Shouldn't be null at least");
            } catch (HttpRequestException e) {
                Assert.AreNotEqual((int) e.Data["StatusCode"], (int) HttpStatusCode.BadRequest, "Shouldn't be BadRequest");
                Assert.True((int) e.Data["StatusCode"] >= 300, "Should be larger than 300");
            }
        }

        [Test]
        public async Task GetCvsAuthorizations2()
        {
            try {
                Request.GetCvsAuthorizations request = new Request.GetCvsAuthorizations(
                    "c97b3de0-d02b-4a56-a4b6-d3539153e140"
                ) {
                    After = "4zOt2LPb",
                    PerPage = 42,
                };
                Response.PaginatedCvsAuthorizations response = await request.Send(client);
                Assert.NotNull(response, "Shouldn't be null at least");
            } catch (HttpRequestException e) {
                Assert.AreNotEqual((int) e.Data["StatusCode"], (int) HttpStatusCode.BadRequest, "Shouldn't be BadRequest");
                Assert.True((int) e.Data["StatusCode"] >= 300, "Should be larger than 300");
            }
        }

        [Test]
        public async Task GetCvsAuthorizations3()
        {
            try {
                Request.GetCvsAuthorizations request = new Request.GetCvsAuthorizations(
                    "c97b3de0-d02b-4a56-a4b6-d3539153e140"
                ) {
                    Before = "n",
                    After = "vCf",
                    PerPage = 46,
                };
                Response.PaginatedCvsAuthorizations response = await request.Send(client);
                Assert.NotNull(response, "Shouldn't be null at least");
            } catch (HttpRequestException e) {
                Assert.AreNotEqual((int) e.Data["StatusCode"], (int) HttpStatusCode.BadRequest, "Shouldn't be BadRequest");
                Assert.True((int) e.Data["StatusCode"] >= 300, "Should be larger than 300");
            }
        }

        [Test]
        public async Task GetCvsAuthorizations4()
        {
            try {
                Request.GetCvsAuthorizations request = new Request.GetCvsAuthorizations(
                    "c97b3de0-d02b-4a56-a4b6-d3539153e140"
                ) {
                    Status = "expired",
                    Before = "5",
                    After = "Ws",
                    PerPage = 33,
                };
                Response.PaginatedCvsAuthorizations response = await request.Send(client);
                Assert.NotNull(response, "Shouldn't be null at least");
            } catch (HttpRequestException e) {
                Assert.AreNotEqual((int) e.Data["StatusCode"], (int) HttpStatusCode.BadRequest, "Shouldn't be BadRequest");
                Assert.True((int) e.Data["StatusCode"] >= 300, "Should be larger than 300");
            }
        }

        [Test]
        public async Task GetCvsAuthorizations5()
        {
            try {
                Request.GetCvsAuthorizations request = new Request.GetCvsAuthorizations(
                    "c97b3de0-d02b-4a56-a4b6-d3539153e140"
                ) {
                    CustomerId = "4bc5c764-73d5-4a6e-92f2-35beb6f3bf48",
                    Status = "canceled",
                    Before = "9KHVuXFGK",
                    After = "t4lw",
                    PerPage = 62,
                };
                Response.PaginatedCvsAuthorizations response = await request.Send(client);
                Assert.NotNull(response, "Shouldn't be null at least");
            } catch (HttpRequestException e) {
                Assert.AreNotEqual((int) e.Data["StatusCode"], (int) HttpStatusCode.BadRequest, "Shouldn't be BadRequest");
                Assert.True((int) e.Data["StatusCode"] >= 300, "Should be larger than 300");
            }
        }

    }
}
